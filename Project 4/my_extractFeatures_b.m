function [extracted_features] = my_extractFeatures_b(image, detected_pts)

    % Part B - Nikhil

    % This totally works, but we got shut down.  We need to impliment our
    % own. :(
%     for i = 1:size(detected_pts,1)
%         [feature, ~] = extractFeatures(image, detected_pts(i,:));
%         try
%             extracted_features(i,:) = feature;
%         catch
%             extracted_features(i,:) = zeros(1,size(extracted_features,2));
%         end
%     end
end

